# Attic

The Tk orchestrator (`pipeline_orchestrator_gui.py`) is the canonical GUI
since v4.x. `interface.py` here is the archived PyQt reference surface.
It is not an entry point and is not imported by the active package.
`main.py` forwards to the Tk GUI.
