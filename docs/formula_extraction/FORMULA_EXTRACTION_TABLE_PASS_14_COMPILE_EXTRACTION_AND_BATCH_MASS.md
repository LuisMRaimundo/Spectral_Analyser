# Formula Extraction Table — Pass 14 — Compile Extraction and Batch Mass

Project-owned **`compile_metrics.py`** (primary) and **`audio_analysis/super_audio_analyzer.py`** (`finalize_batch_power_mass_summary` only). Third-party internals (`numpy`, `pandas`, `sklearn`, `openpyxl`, etc.) are black boxes except where this code combines their outputs with explicit closed-form expressions.

| Function / context | Python expression | Mathematical formula | Symbol definitions | Notes |
|---|---|---|---|---|
| `_sum_finite_numeric` (helper used by `extract_density_components_from_per_note_workbook`) | `mask = np.isfinite(num)`; `return num[mask].sum(), mask.sum()` | \(S=\sum_{j\in\mathcal{J}} x_j\), \(n=|\mathcal{J}|\) with \(\mathcal{J}=\{j: x_j\ \text{finite}\}\) | \(x_j\): entries after `pd.to_numeric(..., errors="coerce")` | Used for legacy band column totals. **extracted** |
| `extract_density_component_sum` | `raw_total = float(series[mask].sum())` | \(S=\sum_{j\in\mathcal{M}} a_j\) over masked finite rows | \(a_j\): `Amplitude_raw` (or `Power_raw` in power mode); \(\mathcal{M}\): finite mask ∧ non-negative (linear/log) ∧ optional `include_for_density` mask | **extracted** |
| `extract_density_component_sum` | `wf == "linear"` path | \(D = S\) | \(D\): returned scalar `D` | **extracted** |
| `extract_density_component_sum` | `wf == "log"`; `d_value = np.log10(1.0 + max(0.0, raw_total))` | \(D=\log_{10}(1+\max(S,0))\) | Common log-density transform | **extracted** |
| `extract_density_component_sum` | `wf == "power"` with `Power_raw` | \(D=\sum_j p_j\) for power column | \(p_j\): `Power_raw` | **extracted** |
| `extract_density_component_sum` | `wf == "power"` fallback `series_amp.pow(2)` | \(D=\sum_j a_j^2\) | \(a_j\): `Amplitude_raw` when `Power_raw` absent | **extracted** |
| `extract_density_component_sum` | `use_elementwise` branch `apply_density_metric(..., normalize=False, ...)` | \(D=\texttt{density.apply\_density\_metric}(\mathbf{a},\texttt{weight\_function}=w')\) on masked amplitude vector \(\mathbf{a}\) | \(w'\): operator GUI weight key | Delegates to **`density`** module; **partially extracted** (black-box `apply_density_metric`). |
| `extract_density_components_from_per_note_workbook` | doc / implementation `harmonic_log_amplitude_density` | \(L_H=\log_{10}(1+\max(0,H_{\mathrm{sum}}))\) | \(H_{\mathrm{sum}}=\) `harmonic_amplitude_sum` | **extracted** |
| `extract_density_components_from_per_note_workbook` | `weighted_h = h_f * wH_f` (and `i`, `s`) | \(c_H = H_{\mathrm{sum}}\,w_H\), \(c_I=I_{\mathrm{sum}}\,w_I\), \(c_S=S_{\mathrm{sum}}\,w_S\) | \(w_H,w_I,w_S\): component energy ratios from metadata | **extracted** |
| `extract_density_components_from_per_note_workbook` | `density_sum = sum(finite_contrib)`; `density_log = np.log10(1.0 + max(0.0, density_sum))` | \(S_w=\sum_{k\in\{H,I,S\}} \tilde c_k\) where \(\tilde c_k=c_k\) if finite else omitted; if no finite terms \(S_w=\mathrm{NaN}\); \(L_w=\log_{10}(1+\max(0,S_w))\) when \(S_w\) finite | Stage-2 legacy weighted log column family | **extracted** |
| `extract_density_components_from_per_note_workbook` | `sum_w = float(w_H)+float(w_I)+float(w_S)`; tolerance check vs `1.0` | \(s_w=w_H+w_I+w_S\); audit flag if \(|s_w-1|>\tau\) | \(\tau=\) `DENSITY_WEIGHT_SUM_TOLERANCE` | No automatic renormalisation. **extracted** |
| `extract_density_components_from_per_note_workbook` | calls `extract_density_component_sum` for each band | Overwrites \(D_H,D_I,D_S\) with canonical \(D\) per `weight_function` | Same as `extract_density_component_sum` semantics per sheet | **extracted** (composition). Corpus-wide `density_metric_normalized = d_{\mathrm{raw}}/\max_j d_{\mathrm{raw},j}` is applied later in the same module’s sheet builder (not recomputed inside this function). |
| `apply_frequency_dependent_normalization` | piecewise `alpha_effective` from `get_frequency_dependent_alpha(freq)` or `alpha` | \(\alpha_{\mathrm{eff}}\) is either register table lookup or constant \(\alpha\) argument | | **extracted** (branching). |
| `apply_frequency_dependent_normalization` | `expected_sum = (N**(1-alpha_effective)-1)/(1-alpha_effective)` when `alpha_effective > 1` | \(E(N)=\dfrac{N^{1-\alpha}-1}{1-\alpha}\) for harmonic-count \(N>0\) | \(N=\) `harmonic_count`; \(\alpha=\) `alpha_effective` | Riemann partial-sum proxy for \(\sum_{n=1}^N n^{-\alpha}\). **extracted** |
| `apply_frequency_dependent_normalization` | `else: expected_sum = np.log(N + 1.0)` | \(E(N)=\log(N+1)\) when \(\alpha_{\mathrm{eff}}\le 1\) | | **heuristic** fallback branch. **extracted** |
| `apply_frequency_dependent_normalization` | `normalized_value = density / (expected_sum + 1e-10)` | \(d' = d / (E(N)+\varepsilon)\) with \(\varepsilon=10^{-10}\) | \(d=\) input density | Per-row loop. **extracted** |
| `apply_frequency_dependent_normalization` | early exits `density <= 0` or bad `freq` / `harm_count` | Returns original \(d\) unchanged | | **extracted** (guard logic). |
| `get_frequency_dependent_alpha` | thresholds on `f0` returning `1.2`, `1.3`, `1.4`, `1.6` | Piecewise constant \(\alpha(f_0)\) on Hz intervals \((-\infty,100),[100,200),[200,400),[400,\infty)\) | | **model-dependent** register policy. **extracted** |
| `_minmax` | `(s - lo) / (hi - lo)` | \(z_i=\dfrac{x_i-x_{\min}}{x_{\max}-x_{\min}}\) when \(x_{\max}>x_{\min}\) both finite | \(x_i\): coerced numeric series | Else zeros or NaN edge cases per code. **extracted** |
| `_robust_normalize_series` | `robust_normalize(values, method=method, clip_range=(0.0, 1.0))` | \(z = \texttt{data\_integrity.robust\_normalize}(\mathbf{x},\texttt{method},[0,1])\) | `method` ∈ `percentile` / `iqr` / `robust_zscore` | **partially extracted** (black-box). |
| `_robust_normalize_series` | `ImportError` fallback `return _minmax(series)` | Min–max as above | | **extracted** (fallback path). |
| `note_to_midi` | `return (octv + 1) * 12 + semi` | MIDI-like index \(m=(o+1)\cdot 12+s\) | \(o=\) parsed octave int; \(s\in[0,11]\) chroma from `_PITCH_TO_SEMITONE`; invalid \(\Rightarrow 10^9\) sentinel | Differs from strict MIDI middle-C convention in other modules; used for **sorting**. **model-dependent** labelling. **extracted** |
| `note_to_fundamental_freq` | `midi_note = (octave + 1) * 12 + semitone_in_octave`; `freq = 440.0 * (2.0 ** ((midi_note - 69) / 12.0))` | \(m=(o+1)12+s\), \(f=440\cdot2^{(m-69)/12}\) Hz | \(o,s\) from regex + `note_map` | Equal temperament, A4=69 in this mapping. **extracted** |
| `apply_weighted_index` | `out["N_harm_norm"] = _robust_normalize_series(_safe("Harmonic Count"), ...)` | \(\tilde N = \texttt{robust\_normalize}(\mathbf{N}_{\mathrm{harm}})\) | | **partially extracted**. |
| `apply_weighted_index` | `valid_mask` conjunction for regression | Boolean mask: finite positive density, harmonic count, fundamental freq | | **extracted** (logic). |
| `apply_weighted_index` | `log_density = np.log(density_metric_raw[valid_mask])`; `log_freq = np.log(fundamental_freqs[valid_mask])`; `alpha = np.cov(log_density, log_freq)[0,1]/np.var(log_freq)` | OLS slope \(\hat\beta=\mathrm{Cov}(U,V)/\mathrm{Var}(V)\) with \(U=\ln d\), \(V=\ln f\) | Sample-restricted to `valid_mask` | **partially extracted** (`numpy` cov/var as black boxes). |
| `apply_weighted_index` | `alpha = max(-1.5, min(-0.3, alpha))` | \(\alpha \leftarrow \mathrm{clip}(\hat\beta,[-1.5,-0.3])\) | | **heuristic** clamp. **extracted** |
| `apply_weighted_index` | `c = log_density.mean() - alpha * log_freq.mean()` | Intercept \(\hat c=\bar U-\alpha\bar V\) | | **extracted** |
| `apply_weighted_index` | `expected_density = np.exp(alpha * np.log(freq) + c)`; per-row ratio | \(d^{\mathrm{freq}}_i = d_i / (\exp(\alpha\ln f_i+c)+\varepsilon)\), \(\varepsilon=10^{-10}\) | Multiplicative frequency debiasing | **extracted** |
| `apply_weighted_index` | `dagn.clip(0.0, 1.0)`; `pnorm.clip(0.0, 1.0)` | Clip \(x\leftarrow \min(1,\max(0,x))\) for `D_agn`, `P_norm` | | **extracted** |
| `apply_weighted_index` | `comb_n = (x - lo) / (hi - lo)` on `Combined Density Metric` | Min–max normalisation of combined metric when higher-priority norm columns absent | | **extracted** |
| `apply_weighted_index` | `_weighted_index_available_terms` | \(I_i=\dfrac{\sum_k w_k\, \tilde{x}_{k,i}\,\mathbf{1}_{k,i}}{\sum_k w_k\,\mathbf{1}_{k,i}}\) with missing not counted as zero in denominator | \(\tilde{x}_{k,i}\): component column; \(w_k\): scheme weights; \(\mathbf{1}_{k,i}\) if finite | Renormalise over **available** terms only. **extracted** |
| `apply_weighted_index` | scheme `"pdf"` weights | \(w=(0.10,0.40,0.30,0.15,0.05)\) on (`Density Metric_Norm2`, `D_agn`, `N_harm_norm`, `Combined`, `P_norm`) internal columns | | **model-dependent** index recipe. **extracted** |
| `apply_weighted_index` | scheme `"current"`; `_D_agn_inv_for_index = (1.0 - dagn).clip(0,1)` | Uses \((1-D_{\mathrm{agn}})\) clipped; weights \((0.40,0.35,0.15,0.10)\) | | **extracted** |
| `apply_weighted_index` | `out["Index_Weighted"].clip(0.0, 1.0)` | Final index clipped to \([0,1]\) | | **extracted** |
| `extract_dissonance_metrics` | `"Dissonance" in column`; `valid.iloc[0]` | For each sheet/column whose name contains substring `"Dissonance"`, export scalar from first non-NaN row | | Same contract as **Pass 12**; **no additional** closed-form physics. **extracted** (heuristic contract); cross-ref Pass 12. |
| `_append_dissonance_excel_sheets` | orchestration: `build_canonical_dissonance_frame`, `to_excel`, `build_dissonance_correlation_matrix(...)` | No project-owned scalar formula beyond delegations and `min_samples` threshold passed through | | **no formula-bearing expression identified** in this function body (I/O + helpers). |
| `finalize_batch_power_mass_summary` | `h = max(0.0, float(harmonic_power_mass))` (and `i`, `s`) | \(h=\max(0,H)\), \(i=\max(0,I)\), \(s=\max(0,S)\) for inputs \(H,I,S\) | Linear power masses | **extracted** |
| `finalize_batch_power_mass_summary` | `total_inharmonic_power_mass = i + s`; `total_power_mass = h + i + s` | \(T_{\mathrm{inh}}=i+s\), \(T=h+i+s\) | | **extracted** |
| `finalize_batch_power_mass_summary` | `hp = 100.0 * h / total_power_mass` (etc.) | \(p_h=100\,h/T,\ p_i=100\,i/T,\ p_s=100\,s/T\) when \(T>0\) | Percentages before renormalisation | **extracted** |
| `finalize_batch_power_mass_summary` | `ssum = hp + ip + sp`; `scale = 100.0 / ssum`; multiply each by `scale`; `tip = ip + sp` | If \(s_{\%}=p_h+p_i+p_s>0\), scaled \(\tilde p_k = 100\,p_k/s_{\%}\) so \(\tilde p_h+\tilde p_i+\tilde p_s=100\); `total_inharmonic_power_percent` \(=\tilde p_i+\tilde p_s\) | Corrects floating drift so trio sums to **exactly** 100 | **extracted** |
| `finalize_batch_power_mass_summary` | `total_power_mass <= 0` branch | All percents set to `0.0` | | **extracted** |

## Summary

- **Fully extracted:** `extract_density_component_sum` (linear / log / power / squared fallback / masked sum); `extract_density_components_from_per_note_workbook` (log harmonic density, weighted amplitude products, weighted-sum log, weight-sum audit, delegation to `extract_density_component_sum`; documents canonical \(\,D_H w_H+D_I w_I+D_S w_S\,\) compile metric); `get_frequency_dependent_alpha`; `apply_frequency_dependent_normalization` (expected-sum scaling, guards); `_minmax`; `note_to_midi`; `note_to_fundamental_freq`; `apply_weighted_index` (log–log regression slope with clamp, exponential debiasing, clipping, combined min–max fallback, `_weighted_index_available_terms` rational, both weight schemes, final clip); `finalize_batch_power_mass_summary` (nonnegative masses, percentages, renormalisation to 100, total inharmonic percent); `_sum_finite_numeric`.

- **Partially extracted:** `extract_density_component_sum` when routed to `density.apply_density_metric`; `_robust_normalize_series` via `data_integrity.robust_normalize`; `apply_weighted_index` sample covariance / variance step.

- **Functions not found:** All listed targets exist in the repository paths inspected.

- **Ambiguous or model-dependent items requiring human review:** (1) `note_to_midi` sentinel \(10^9\) for invalid notes vs strict MIDI. (2) Piecewise \(\alpha(f_0)\) and OLS clamp \([-1.5,-0.3]\) — policy, not unique mathematics. (3) Index weight schemes `"pdf"` vs `"current"`. (4) `extract_dissonance_metrics` substring `"Dissonance"` and first-row pick — export contract. (5) Register-dependent normalisation using integral proxy \(E(N)\) vs discrete partial sum.

- **Recommendation:** A **Pass 14 validation-plan** document is **recommended** so tests can pin compile-time sums, batch percent renormalisation, weighted-index algebra (including available-term renormalisation), and note→frequency mapping without coupling to full Excel GUI flows.
