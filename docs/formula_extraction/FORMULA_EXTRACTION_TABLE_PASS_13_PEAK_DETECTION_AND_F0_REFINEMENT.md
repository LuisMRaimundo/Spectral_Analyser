# Formula Extraction Table — Pass 13 — Peak Detection and f0 Refinement

Project-owned `proc_audio.py` expressions only (peak refinement, prominence, local metrics, f0 correction, note↔frequency). Third-party library internals (`numpy`, `scipy`, `pandas`, etc.) are treated as black boxes; only how this file *calls* them is described.

| Function / context | Python expression | Mathematical formula | Symbol definitions | Notes |
|---|---|---|---|---|
| `_parabolic_peak` | `alpha, beta, gamma = y[x-1], y[x], y[x+1]`; `denom = alpha - 2*beta + gamma`; `p = 0.5 * (alpha - gamma) / denom`; `xv = x + p`; `yv = beta - 0.25 * (alpha - gamma) * p` | Fit a quadratic through \(({-1},\alpha),(0,\beta),(1,\gamma)\) in relative bin coordinates; sub-bin offset \(p=\dfrac{\alpha-\gamma}{2(\alpha-2\beta+\gamma)}\); refined abscissa \(x_{\mathrm{v}}=x+p\); ordinate \(y_{\mathrm{v}}=\beta-\tfrac14(\alpha-\gamma)p\) | \(y_k\): samples at integer bins; \(x\): peak bin index; edge return \((x,y_x)\) if \(x\le 0\) or \(x\ge N-1\); if \(\alpha-2\beta+\gamma=0\), return \((x,\beta)\) | QIFFT-style parabolic interpolation on **linear** ordinates (not log). **extracted** |
| `_parabolic_interpolation_log_magnitude` | `log_mags = 20 * np.log10(np.maximum(magnitudes, 1e-10))` | \(Y_k = 20\log_{10}\max(M_k,\varepsilon)\) with \(\varepsilon=10^{-10}\) | \(M_k\): linear magnitudes | Log floor avoids \(-\infty\); **model-dependent** / numerical. **extracted** |
| `_parabolic_interpolation_log_magnitude` | `y1, y2, y3 = log_mags[k-1], log_mags[k], log_mags[k+1]`; `a = (y1 - 2*y2 + y3) / 2.0`; `b = (y3 - y1) / 2.0`; `x_peak = -b / (2 * a)` | Parabola \(Y(x)=ax^2+bx+c\) on relative bins \(x\in\{-1,0,1\}\): \(a=\tfrac12(Y_1-2Y_2+Y_3)\), \(b=\tfrac12(Y_3-Y_1)\); vertex offset \(x_{\mathrm{peak}}=-b/(2a)\) | \(k=\) `peak_idx`; if \(|a|<10^{-10}\), set \(x_{\mathrm{peak}}=0\) | Standard three-point parabola in **log-magnitude** domain. **extracted** |
| `_parabolic_interpolation_log_magnitude` | `if abs(x_peak) > 0.5: ... return freq_base + peak_idx * bin_spacing, False` | Reject interpolation if \(|x_{\mathrm{peak}}|> \tfrac12\) bin; fallback frequency \(f=f_0+k\,\Delta f\) | \(f_0=\) `freq_base` (first bin frequency); \(\Delta f=\) `bin_spacing`; \(k=\) `peak_idx` | **Heuristic** validity cap. **extracted** |
| `_parabolic_interpolation_log_magnitude` | `freq_bin = freq_base + peak_idx * bin_spacing`; `freq_corrected = freq_bin + x_peak * bin_spacing` | \(f_{\mathrm{bin}}=f_0+k\Delta f\); \(f_{\mathrm{corr}}=f_{\mathrm{bin}}+x_{\mathrm{peak}}\Delta f\) | Same symbols | Sub-bin correction along a uniform grid. **extracted** |
| `_refine_peak_index` | `lo = max(0, approx_idx - refine_radius)`; `hi = min(n, approx_idx + refine_radius + 1)`; `return lo + int(np.argmax(magnitudes[lo:hi]))` | \(k^\*=\arg\max_{\,i\in[\ell,h)} M_i\) with \(\ell=\max(0,k_0-r)\), \(h=\min(N,k_0+r+1)\); return \(\ell+k^\*\) in global index | \(k_0=\) `approx_idx`; \(r=\) `refine_radius` (default 2); \(M_i\) linear magnitudes | Snap to strongest bin in window; ties broken by **first** maximum (`argmax` convention). **ambiguous / needs human review** (tie policy). **extracted** |
| `_infer_bin_spacing_from_freqs` | `diffs = np.diff(freqs)`; `diffs = diffs[np.isfinite(diffs) & (diffs > 0)]`; `return float(np.median(diffs))` | \(\Delta f = \mathrm{median}\{f_{i+1}-f_i : \Delta_i>0,\ \Delta_i\ \text{finite}\}\) | \(f_i\): monotone frequency axis samples | Uses `numpy` median on positive finite gaps; **heuristic** if axis is non-uniform. **partially extracted** (black-box `median`). |
| `_refine_candidate_to_interpolated_peak` | `idx0 = int(np.argmin(np.abs(freqs - candidate_freq_hz)))` | \(k_0=\arg\min_i |f_i - f_{\mathrm{cand}}|\) | \(f_{\mathrm{cand}}=\) `candidate_freq_hz` | Nearest-bin anchor before local snap. **extracted** |
| `_refine_candidate_to_interpolated_peak` | `mag_db = float(20.0 * np.log10(max(amp, 1e-12)))` | \(L_{\mathrm{dB}}=20\log_{10}\max(A,10^{-12})\) | \(A\): linear magnitude at refined bin | Floor \(10^{-12}\). **extracted** |
| `_refine_candidate_to_interpolated_peak` | `offset_bins = (freq_interp - bin_center) / bin_spacing` | \(\delta_{\mathrm{bin}}=(\hat f - f_{\mathrm{center}})/\Delta f\) | \(\hat f=\) interpolated frequency; \(f_{\mathrm{center}}\): bin-centre frequency | Audited sub-bin offset. **extracted** |
| `_saddle_prominence_db` | `peak_db = 20.0 * np.log10(max(mags[peak_idx], 1e-10))`; `left_min_db = 20.0 * np.log10(max(min(left_slice), 1e-10))`; `right_min_db = ...`; `return peak_db - max(left_min_db, right_min_db)` | \(L_{\mathrm{p}}=20\log_{10}\max(M_p,\varepsilon)\); \(L_\ell=20\log_{10}\max(\min_{i\in W_\ell} M_i,\varepsilon)\); \(L_r=20\log_{10}\max(\min_{j\in W_r} M_j,\varepsilon)\); \(\mathrm{Prom}_{\mathrm{dB}}=L_p-\max(L_\ell,L_r)\) | \(p=\) `peak_idx`; \(W_\ell=[\max(0,p-W),\,p)\); \(W_r=(p,\,\min(N,p+W+1))\); \(W=\) `saddle_window` (default 10); \(\varepsilon=10^{-10}\) | “Saddle” prominence vs higher of flank minima; returns \(-\infty\) on bad geometry. **extracted**; window width **model-dependent** / documented heuristic. |
| `_is_local_peak_valid` | `peak_idx = _refine_peak_index(...)` then `log_mags = 20 * np.log10(np.maximum(magnitudes, 1e-10))` | Same refinement + log map as above | | Composed gates. **extracted** |
| `_is_local_peak_valid` | `is_peak = peak_mag_db > left_db and peak_mag_db > right_db` | Strict local maximum on **dB** samples: \(Y_p>Y_{p-1}\) and \(Y_p>Y_{p+1}\) | \(Y_k=20\log_{10}\max(M_k,10^{-10})\) | Neighbour check in dB domain. **extracted** |
| `_is_local_peak_valid` | `threshold_met = prom_db >= float(threshold_db)` | \(\mathrm{Prom}_{\mathrm{dB}}\ge T_{\mathrm{prom}}\) | Default \(T_{\mathrm{prom}}=3\) dB | Saddle prominence gate. **extracted** |
| `_is_local_peak_valid` | `noise_floor_mag = np.percentile(local_magnitudes, noise_floor_percentile)`; `noise_floor_db = 20 * np.log10(noise_floor_mag + 1e-10)`; `snr_db = peak_mag_db - noise_floor_db`; `snr_valid = snr_db >= 3.0` | \(Q_p=\) \(p_{\mathrm{nf}}\)-th percentile of \(\{M_i\}\) over \(i\in[p-H,p+H)\); \(N_{\mathrm{dB}}=20\log_{10}(Q_p+10^{-10})\); \(\mathrm{SNR}_{\mathrm{dB}}=Y_p-N_{\mathrm{dB}}\); require \(\mathrm{SNR}_{\mathrm{dB}}\ge 3\) | \(p_{\mathrm{nf}}=\) `noise_floor_percentile` (default 15); \(H=\) `window_size` (default 50) | Percentile via `numpy` (black box); fixed **3 dB** SNR gate (hard-coded). **partially extracted** |
| `_local_peak_metrics` | linear `peak_db`, `left_db`, `right_db` with `max(..., 1e-10)` | Local-max test on dB from **floored linear** mags: \(Y_p>Y_{p\pm 1}\) with \(Y=20\log_{10}\max(M,10^{-10})\) | | Differs from `_is_local_peak_valid` which uses `np.maximum` on full array then indexes—same floor value. **extracted** |
| `_local_peak_metrics` | `prominence_db = _saddle_prominence_db(...)` | \(\mathrm{Prom}_{\mathrm{dB}}\) as in `_saddle_prominence_db` | | **extracted** |
| `_local_peak_metrics` | `nf_mag = float(np.percentile(mags[s:e], noise_floor_percentile))`; `nf_db = 20.0 * np.log10(max(nf_mag, 1e-10))`; `snr_db = peak_db - nf_db` | Same SNR structure as `_is_local_peak_valid` but percentile on linear \(M\) in \([s,e)\) | \(s=\max(0,p-H)\), \(e=\min(N,p+H)\) | **extracted** / **partially extracted** (percentile black box). |
| `_correct_f0_candidate_against_prior` | `candidates.append((cand / float(r), 1.0 / float(r)))`; `candidates.append((cand * float(r), float(r)))` for `r in range(2, max_harmonic_ratio+1)` | Candidate set \(\mathcal{C}=\{c\}\cup\{c/r, c\cdot r : r=2,\ldots,R\}\) with tags \(1,\,1/r,\,r\) | \(c=\) `candidate_hz`; prior \(f_{\mathrm{prior}}=\) `prior_hz`; \(R=\) `max_harmonic_ratio` (default 6) | Integer harmonic/subharmonic hypotheses. **extracted** |
| `_correct_f0_candidate_against_prior` | `err = abs(1200.0 * np.log2(hz / prior))` | Cent deviation \(\varepsilon(f)=1200\,\bigl|\log_2(f/f_{\mathrm{prior}})\bigr|\) | Choose \(f\in\mathcal{C}\) minimising \(\varepsilon(f)\) | Twelve-tone equal-temperament cents. **extracted** |
| `_calculate_bin_spacing` | `n_fft_effective = n_fft * zero_padding`; `bin_spacing = sr / n_fft_effective` | \(\Delta f = \dfrac{f_s}{N_{\mathrm{fft}}\,Z}\) | \(f_s=\) `sr`; \(N_{\mathrm{fft}}=\) `n_fft`; \(Z=\) `zero_padding` (effective length factor) | Uniform DFT bin spacing. **extracted** |
| `frequency_to_note_name` | `midi = 69.0 + 12.0 * math.log2(f / a4)` | Continuous MIDI number \(m = 69 + 12\log_2(f/f_{A4})\) | \(f=\) `freq_hz`; \(f_{A4}=\) `a4` (default 440 Hz) | **extracted** |
| `frequency_to_note_name` | `midi_round = int(round(midi))`; `f_ref = a4 * (2.0 ** ((midi_round - 69) / 12.0))` | Nearest chroma MIDI \(\hat m=\mathrm{round}(m)\); reference Hz \(f_{\mathrm{ref}}=f_{A4}\,2^{(\hat m-69)/12}\) | | Tie at half-semitone not specified beyond `round`. **ambiguous / needs human review** (`.5` rounding). **extracted** |
| `frequency_to_note_name` | `cents = 1200.0 * math.log2(f / f_ref)` | \(\mathrm{cents}=1200\log_2(f/f_{\mathrm{ref}})\) | Signed deviation from tempered pitch class | **extracted** |
| `AudioProcessor.calculate_fundamental_frequency` | `f_hz = float(s_num)` when numeric string | If input parses as positive Hz, \(f=f_{\mathrm{num}}\) | Fast path | **extracted** (trivial identity). |
| `AudioProcessor.calculate_fundamental_frequency` | `freq_C0 = freq_A4 * 2 ** (-4.75)` with `freq_A4 = 440.0` | \(f_{C_0}=440\cdot 2^{-57/12}=440\cdot2^{-4.75}\) (Hz) when \(f_{A4}=440\) | Fixed anchor; \(-4.75=-57/12\) semitones from \(C_0\) to \(A_4\) on MIDI scale | **extracted** |
| `AudioProcessor.calculate_fundamental_frequency` | `h = idx + 12 * octave`; `f = freq_C0 * (2 ** (h / 12.0))` | Semitone index from \(C_0\): \(h=i+12o\); \(f=f_{C_0}\,2^{h/12}\) | \(i=\) chromatic index of pitch class in `names` starting at C=0; \(o=\) signed octave digit | Equivalent to \(f=440\cdot2^{(m-69)/12}\) with \(m=h+12\) (MIDI). **extracted** |
| `AudioProcessor.calculate_fundamental_frequency` (equivalent form) | (algebraic equivalence to standard) | \(f = f_{A4}\,2^{(m-69)/12}\) with integer MIDI \(m\) implied by parsed note | \(f_{A4}=440\) Hz in this implementation | User-requested closed form; matches equal temperament. **extracted** |

## Summary

- **Functions fully extracted (formula-bearing parts):** `_parabolic_peak`; `_parabolic_interpolation_log_magnitude`; `_refine_peak_index` (argmax window rule); `_saddle_prominence_db`; `_local_peak_metrics`; `_is_local_peak_valid` (combined gates, including hard-coded 3 dB SNR check); `_correct_f0_candidate_against_prior`; `_calculate_bin_spacing`; `frequency_to_note_name` (MIDI, reference frequency, cents); `AudioProcessor.calculate_fundamental_frequency` (numeric Hz path, \(f_{C_0}\) anchor, \(f=f_{C_0}2^{h/12}\)); `_infer_bin_spacing_from_freqs` (median-positive-diff spacing); `_refine_candidate_to_interpolated_peak` (nearest bin, dB magnitude, sub-bin offset).

- **Functions partially extracted:** `_infer_bin_spacing_from_freqs` and percentile-based noise floors (dependency on `numpy` `median` / `percentile` ordering and interpolation as black boxes).

- **Functions not found:** None of the named targets were missing from `proc_audio.py` in the inspected regions.

- **Ambiguous or model-dependent items requiring human review:** (1) `_refine_peak_index` tie-breaking when multiple bins share the same maximum magnitude. (2) `frequency_to_note_name` behaviour exactly at half-semitone boundaries (`round`). (3) Log/linear floors (`1e-10`, `1e-12`) and dB vs linear local-max consistency across helpers—small numerical shifts, not alternate physics. (4) `saddle_window`, `refine_radius`, percentile rank, and fixed `3.0` dB SNR gates are **heuristic / policy** choices documented in code comments, not unique mathematical necessities.

- **Recommendation:** A dedicated **validation-plan** document for Pass 13 is **recommended**, so formula-validation tests can pin parabolic refiners, saddle prominence, SNR gates, cents-based prior correction, and tempered-frequency conversions without conflating them with unrelated `proc_audio` passes.
