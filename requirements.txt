# SoundSpectrAnalyse - Production Dependencies
# Pinned versions for reproducibility

# Core scientific computing
numpy>=1.21.0,<2.0.0
pandas>=1.3.0,<2.0.0
scipy>=1.7.0,<2.0.0

# Audio processing
librosa>=0.9.0,<1.0.0
soundfile>=0.10.0,<1.0.0
pydub>=0.25.0,<1.0.0  # Used by audio_utils load fallback (requires ffmpeg on PATH for some formats)
numba>=0.56.0,<0.60.0  # Compatible with librosa

# Machine learning
scikit-learn>=1.0.0,<2.0.0
umap-learn>=0.5.0,<1.0.0

# GUI
PyQt5>=5.15.0,<6.0.0
# Alternative: PySide6>=6.2.0,<7.0.0

# Visualization
matplotlib>=3.5.0,<4.0.0
seaborn>=0.12.0,<1.0.0
plotly>=5.0.0,<6.0.0

# Excel I/O
openpyxl>=3.0.0,<4.0.0
xlsxwriter>=3.0.0,<4.0.0

# HTTP requests (optional; some auxiliary scripts)
requests>=2.28.0,<3.0.0

# Optional: Environment variable management
python-dotenv>=0.19.0,<1.0.0  # Recommended for .env file support

# NOTE: Testing dependencies (pytest, pytest-cov, coverage) moved to requirements-dev.txt
# Install dev dependencies with: pip install -r requirements-dev.txt
