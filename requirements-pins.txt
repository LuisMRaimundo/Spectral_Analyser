# Optional exact pins for a reproducible lab environment (does not change Excel/JSON schemas).
# Must stay within the upper bounds in requirements.txt / pyproject.toml (e.g. numpy<2, pandas<2).
#
# Typical workflow:
#   pip install -r requirements.txt && pip install -r requirements-dev.txt
#   pip install --upgrade --force-reinstall -r requirements-pins.txt
#
# Use for thesis appendices / CI matrix debugging — not required for normal runs.

numpy==1.26.4
pandas==1.5.3
scipy==1.11.4
librosa==0.10.2.post1
scikit-learn==1.3.2
matplotlib==3.8.4
seaborn==0.13.2
plotly==5.24.1
PyQt5==5.15.11
soundfile==0.12.1
numba==0.59.1
openpyxl==3.1.5
xlsxwriter==3.2.0
requests==2.31.0
python-dotenv==1.0.1
pydub==0.25.1
umap-learn==0.5.6
